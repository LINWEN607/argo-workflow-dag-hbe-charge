apiVersion: v1
kind: Secret
metadata:
  name: gitlab-webhook-secret
  namespace: argo-cicd
type: Opaque
data:
  # GitLab webhook secret token (base64编码)
  # 示例: echo -n 'your_gitlab_webhook_secret_token' | base64
  secretToken: Y2ljZA==