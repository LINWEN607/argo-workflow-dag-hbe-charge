apiVersion: argoproj.io/v1alpha1
kind: EventBus
metadata:
  name: default
  namespace: argo-cicd
spec:
  nats:
    native:
      replicas: 1
      auth: token
      volumeClaimTemplates:
        - metadata:
            name: stan-pv-0
          spec:
            accessModes:
              - ReadWriteOnce
            resources:
              requests:
                storage: 1Gi
            storageClassName: nfs-client