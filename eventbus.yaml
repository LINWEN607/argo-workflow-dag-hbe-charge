apiVersion: argoproj.io/v1alpha1
kind: EventBus
metadata:
  name: default
  namespace: argo-cicd
spec:
  nats:
    native:
      replicas: 1
      auth: token
      persistence:
        storageClassName: nfs-client
        accessMode: ReadWriteOnce
        volumeSize: 1Gi